/** Single-currency v1: all monetary values are stored and interpreted as GBP. */
export const DEFAULT_CURRENCY = "GBP" as const;

/** Timestamps are stored in UTC; the UI displays them in this timezone. */
export const APP_TIME_ZONE = "Europe/London" as const;
