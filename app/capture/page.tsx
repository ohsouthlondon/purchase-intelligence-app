import { PlaceholderScreen } from "@/components/placeholder-screen";

export default function CapturePage() {
  return (
    <PlaceholderScreen
      title="Capture"
      description="Add a purchase manually or upload a receipt photo."
      milestone="Manual entry (Slice 3) and receipt upload (Slice 4) land here"
    />
  );
}
